---
const {name, img, description} = Astro.props;
---

<article class="card">
    <div class="card-text">
      <div class="name">{name}</div>
      <p>
        {description}
      </p>
    </div>
    <div class="card-image">
      <div class="name">{name}</div>
      <img src={img} alt="Nombre del miembro del equipo">
    </div>
</article>

<style is:global>
  .card {
    position: relative;
    display: flex;
    background-color: white;
    border-radius: 8px;
    height: auto;
    width: 70%;
    height: 350px;
    box-shadow: 0px 4px 6px rgba(0, 0, 0, 0.1);
  }

  .card-text {
    background-color: #1e3d37;
    color: white;
    padding: 20px;
    position: relative;
    flex: 1;
    align-items: center;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
  }

  .name {
    background-color: #f4b63d;
    color: black;
    font-weight: normal;
    font-size: 20px;
    padding: 15px;
    display: inline-block;
    position: absolute;
    top: -16px;
    left: -16px;
    z-index: 99;
  }

  .card-text p {
    margin-top: 15px;
    font-size: 16px;
    line-height: 1.5;
  }

  .card-image {
    width: 50%;
    max-width: 700px;
  }

  .card-image img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position:top;
    border-radius: 0 8px 8px 0;
  }


  @media (max-width: 480px) {
    .card-image {
      height: 120px;
      border-radius: 5px;
      width: 100%;
    }

    .card{
    display: flex;
    width: 90%;
    height: 300px;
    flex-direction: column-reverse;
    align-items: center;
    justify-content: center;
    }

    .card-text {
      padding: 0px 15px;
      flex: 1;
      height: 300px;
    }

    .card-text > .name {
      display: none;
    }

    .card-image > .name{
    }

    .card-image > img{
      height: 120px;
      width: 100%;
      object-fit: cover;
      object-position: top;
      border-radius: 5px 5px 0px 0px;
    }


    .name {
      font-size: 16px;
      padding: 4px 8px;
      top: -10px;
      left: -10px;
    }

    .card-text p {
      font-size: 12px;
    }
  }
</style>